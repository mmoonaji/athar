# Features (Organisms & Contextual Modules)

This folder contains components, hooks, and helpers grouped by functional feature domain (e.g. `lessons`, `onboarding`, `streak`).

Each subfolder contains feature-specific UI, feature-specific local state, and logical boundaries, keeping files modular.
