# Reusable Shared UI Components

This folder contains global, reusable presentational UI components (atoms/molecules/compound primitives) built with `tailwind` and `shadcn/ui` (e.g. Button, Input, Modal). 

They contain no direct business logic.
